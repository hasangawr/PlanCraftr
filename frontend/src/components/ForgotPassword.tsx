import { Box, Typography } from '@mui/material';

const ForgotPassword = () => {
  return (
    <Box>
      <Typography variant="h5">Forgot password component</Typography>
    </Box>
  );
};

export default ForgotPassword;
